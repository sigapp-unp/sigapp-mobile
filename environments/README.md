# Environment Config

## Setup

```bash
cp environments/local.env.example environments/local.env
cp environments/prod.env.example environments/prod.env
```

## Auto-loading

- `kDebugMode` → `environments/local.env`
- `kReleaseMode` → `environments/prod.env`
